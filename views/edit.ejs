<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-zinc-100 to-zinc-200 min-h-screen font-['Poppins']">
    <nav class="bg-emerald-600 p-4 fixed top-0 w-full shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <span class="text-white text-xl font-bold hover:scale-105 transition-transform duration-300">NoteNest</span>
            <a href="/" class="text-white text-lg hover:text-emerald-200 transition-all duration-300 hover:scale-105 hover:rotate-2">Home</a>
        </div>
    </nav>
    <div class="container mx-auto px-4 flex items-center justify-center min-h-screen">
        <div class="bg-zinc-50 p-8 rounded-xl shadow-2xl w-full max-w-md transform hover:scale-[1.01] transition-transform duration-300 mt-16 backdrop-blur-sm bg-opacity-90">
            <h1 class="text-3xl font-bold mb-8 text-center text-zinc-800 animate-fade-in">Edit File</h1>
            <form action="/uploads/<%= file.folder %>/<%= file.title %>/update" method="POST" class="space-y-6">
                <div class="transform transition-all duration-300 hover:translate-x-1">
                    <label for="title" class="block text-sm font-medium text-zinc-700 mb-2">File Title</label>
                    <input type="text" id="title" name="title" value="<%= file.title %>" required 
                           class="mt-1 block w-full rounded-lg border-zinc-300 shadow-sm 
                                  focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500 
                                  transition-all duration-300 hover:shadow-md bg-zinc-50 px-4 py-2">
                </div>
                <div class="transform transition-all duration-300 hover:translate-x-1">
                    <label for="content" class="block text-sm font-medium text-zinc-700 mb-2">File Content</label>
                    <textarea id="content" name="content" rows="10" required 
                              class="mt-1 block w-full rounded-lg border-zinc-300 shadow-sm 
                                     focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500 
                                     transition-all duration-300 hover:shadow-md bg-zinc-50 px-4 py-2"><%= file.content %></textarea>
                </div>
                <div class="mt-8">
                    <button type="submit" 
                            class="w-full flex justify-center py-3 px-4 border border-transparent 
                                   rounded-lg shadow-sm text-sm font-medium text-white 
                                   bg-gradient-to-r from-emerald-600 to-teal-600
                                   hover:from-emerald-500 hover:to-teal-500
                                   transform transition-all duration-300 
                                   hover:scale-105 hover:shadow-lg 
                                   focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-emerald-500
                                   active:scale-95">
                        Update File
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
